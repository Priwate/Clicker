
class Category {
  constructor(id, title, level, resource, equipment, hits, value ){
    this.id=id,
    this.title=title,
    this.level=level,
    this.resource=resource,
    this.equipment=equipment,
    this.hits=hits,
    this.value=value
  }
}

export default Category;